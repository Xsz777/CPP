function [position, orientation, objects] = grab(position, orientation, objects,index)%This is a TO-DO function
    position = position;
    orientation = orientation;
    objects(index, :) = [];
end